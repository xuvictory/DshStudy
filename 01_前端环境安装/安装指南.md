# 1.node.js安装

https://mirrors.aliyun.com/nodejs-release/

安装之后，自带以下命令：
node -v
npm -v 
npx -v

# 2.pnpm安装

npm install -g pnpm

验证命令：
pnpm -v

# 3.typescript安装

npm install -g typescript

验证命令：
tsc -v